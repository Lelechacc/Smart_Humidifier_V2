# =================================================================================
# 【V8 旗舰版】官方零件自动采购单
# =================================================================================
dependencies:
  # 必须下这个包，否则 bsp_ws2812.c 会报找不到 led_strip.h 的错
  espressif/led_strip: "^2.5.0"
  
  # 限定编译器核心版本
  idf: ">=5.3.1"